# 관리자/직원 화면 공통 위젯

이 폴더는 관리자 및 직원(Employee) 화면에서 사용하는 공통 위젯과 컴포넌트를 포함합니다.
**참고:** Customer 관련 위젯은 `../customer/` 폴더에 있습니다.

## 📁 파일 구조

### 공통 컴포넌트

#### `admin_drawer.dart`
관리자 화면의 사이드 드로워 메뉴 위젯

**주요 클래스:**
- `AdminMenuType`: 관리자 메뉴 타입 enum (주문 관리, 반품 관리 등)
- `AdminDrawerMenuItem`: 드로워 메뉴 아이템 클래스
- `AdminDrawer`: 관리자 드로워 위젯

**사용 예시:**
```dart
drawer: AdminDrawer(
  currentMenu: AdminMenuType.orderManagement,
  userName: '홍길동',
  menuItems: [
    AdminDrawerMenuItem(
      label: '주문 관리',
      icon: Icons.shopping_cart,
      menuType: AdminMenuType.orderManagement,
      onTap: () { /* 네비게이션 */ },
    ),
  ],
  onProfileEditTap: () { /* 개인정보 수정 */ },
),
```

#### `base_order_card.dart`
주문/반품 카드의 공통 베이스 위젯

**주요 클래스:**
- `BaseOrderCard`: 주문 카드 기본 위젯

**특징:**
- 주문 관리와 반품 관리에서 공통으로 사용하는 카드 구조
- 선택 상태, 주문 ID, 고객명, 주문 상태 표시

#### `admin_storage.dart`
관리자 정보 저장소 클래스

**주요 클래스:**
- `AdminStorage`: 관리자(Employee) 정보를 get_storage에 저장하고 불러오는 클래스

**주요 메서드:**
- `saveAdmin(Employee employee)`: 관리자 정보 저장
- `getAdmin()`: 저장된 관리자 정보 가져오기
- `getAdminName()`: 관리자 이름 가져오기
- `clearAdmin()`: 관리자 정보 삭제
- `isAdminLoggedIn()`: 로그인 여부 확인

#### `admin_tablet_utils.dart`
태블릿 관련 유틸리티 함수

**주요 함수:**
- `isTablet(BuildContext context)`: 태블릿 여부 확인
- `lockTabletLandscape(BuildContext context)`: 태블릿 가로 모드 고정
- `unlockAllOrientations()`: 모든 방향 허용으로 복구

#### `order_utils.dart`
주문 관련 유틸리티 함수

**주요 함수:**
- `formatPrice(int price)`: 가격을 천 단위 콤마 형식으로 포맷팅

**사용 예시:**
```dart
final formattedPrice = formatPrice(120000); // "120,000"
```

### 주문 관리용 위젯

#### `order_card.dart`
주문 관리 화면에서 사용하는 주문 카드 위젯

**주요 클래스:**
- `OrderCard`: `BaseOrderCard`를 상속한 주문 카드

**사용 예시:**
```dart
OrderCard(
  orderId: 'ORD001',
  customerName: '홍길동',
  orderStatus: '대기중',
  isSelected: false,
  onTap: () { /* 선택 처리 */ },
)
```

#### `order_detail_view.dart`
주문 상세 정보 뷰

**주요 클래스:**
- `OrderDetailView`: 주문 상세 정보를 표시하는 위젯

**기능:**
- 주문자 정보 표시 (`../customer/customer_info_card.dart`의 `CustomerInfoCard` 사용)
- 주문 상품 목록 표시
- 총 가격 계산 및 표시
- 픽업 완료 버튼

**사용 예시:**
```dart
OrderDetailView(orderId: 'ORD001')
```

### 반품 관리용 위젯

#### `return_order_card.dart`
반품 관리 화면에서 사용하는 반품 주문 카드 위젯

**주요 클래스:**
- `ReturnOrderCard`: `BaseOrderCard`를 상속한 반품 카드

**사용 예시:**
```dart
ReturnOrderCard(
  orderId: 'ORD001',
  customerName: '홍길동',
  orderStatus: '반품요청',
  isSelected: false,
  onTap: () { /* 선택 처리 */ },
)
```

#### `return_order_detail_view.dart`
반품 주문 상세 정보 뷰

**주요 클래스:**
- `ReturnOrderDetailView`: 반품 주문 상세 정보를 표시하는 위젯

**기능:**
- 주문자 정보 표시 (`../customer/customer_info_card.dart`의 `CustomerInfoCard` 사용)
- 반품 상품 목록 표시
- 반품 확인 버튼

**사용 예시:**
```dart
ReturnOrderDetailView(orderId: 'ORD001')
```

## 🔄 공통 사용 패턴

### 카드 위젯 사용
주문 관리와 반품 관리 모두 `BaseOrderCard`를 기반으로 하며, 각각 `OrderCard`와 `ReturnOrderCard`로 구현되어 있습니다.

### 상세 뷰 사용
두 상세 뷰 모두 `../customer/customer_info_card.dart`의 `CustomerInfoCard`를 사용하여 주문자 정보를 표시합니다.

### 유틸리티 함수
가격 포맷팅 등 공통 기능은 `order_utils.dart`에서 제공합니다.

## 📝 참고 사항

- 모든 위젯은 `custom` 폴더의 커스텀 위젯을 사용합니다
- 공통으로 사용 가능한 컴포넌트는 재사용성을 고려하여 설계되었습니다
- 각 위젯은 독립적으로 사용 가능하며, 필요시 확장 가능합니다

